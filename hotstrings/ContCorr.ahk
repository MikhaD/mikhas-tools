; These hotstrings add the apostrophe to contractions
#If tools["ContCorr"] == 1
::arent::aren't

#If tools["ContCorr"] == 1
::cant::can't

#If tools["ContCorr"] == 1
::couldnt::couldn't

#If tools["ContCorr"] == 1
::couldve::could've

#If tools["ContCorr"] == 1
::didnt::didn't

#If tools["ContCorr"] == 1
::doesnt::doesn't

#If tools["ContCorr"] == 1
::dont::don't

#If tools["ContCorr"] == 1
::hadnt::hadn't

#If tools["ContCorr"] == 1
::hasnt::hasn't

#If tools["ContCorr"] == 1
::havent::haven't

#If tools["ContCorr"] == 1
::hed::he'd

#If tools["ContCorr"] == 1
::hes::he's

#If tools["ContCorr"] == 1
::howd::how'd

#If tools["ContCorr"] == 1
::howll::how'll

#If tools["ContCorr"] == 1
::howre::how're

#If tools["ContCorr"] == 1
::hows::how's

#If tools["ContCorr"] == 1
:c:Id::I'd

#If tools["ContCorr"] == 1
:c:Ill::I'll

#If tools["ContCorr"] == 1
:c:Im::I'm

#If tools["ContCorr"] == 1
::isnt::isn't

#If tools["ContCorr"] == 1
::itd::it'd

#If tools["ContCorr"] == 1
::itll::it'll

#If tools["ContCorr"] == 1
::Ive::I've

#If tools["ContCorr"] == 1
::maynt::mayn't

#If tools["ContCorr"] == 1
::mayve::may've

#If tools["ContCorr"] == 1
::mightnt::mightn't

#If tools["ContCorr"] == 1
::mightve::might've

#If tools["ContCorr"] == 1
::mustnt::mustn't

#If tools["ContCorr"] == 1
::mustve::must've

#If tools["ContCorr"] == 1
::neednt::needn't

#If tools["ContCorr"] == 1
::oclock::o'clock

#If tools["ContCorr"] == 1
::oughtnt::oughtn't

#If tools["ContCorr"] == 1
::shant::shan't

#If tools["ContCorr"] == 1
::shes::she's

#If tools["ContCorr"] == 1
::shouldnt::shouldn't

#If tools["ContCorr"] == 1
::shouldve::should've

#If tools["ContCorr"] == 1
::thatd::that'd

#If tools["ContCorr"] == 1
::thatll::that'll

#If tools["ContCorr"] == 1
::thatre::that're

#If tools["ContCorr"] == 1
::thats::that's

#If tools["ContCorr"] == 1
::thered::there'd

#If tools["ContCorr"] == 1
::therere::there're

#If tools["ContCorr"] == 1
::theres::there's

#If tools["ContCorr"] == 1
::thesere::these're

#If tools["ContCorr"] == 1
::theyd::they'd

#If tools["ContCorr"] == 1
::theyll::they'll

#If tools["ContCorr"] == 1
::theyre::they're

#If tools["ContCorr"] == 1
::theyve::they've

#If tools["ContCorr"] == 1
::thiss::this's

#If tools["ContCorr"] == 1
::thosere::those're

#If tools["ContCorr"] == 1
::wasnt::wasn't

#If tools["ContCorr"] == 1
::wed::we'd

#If tools["ContCorr"] == 1
::werent::weren't

#If tools["ContCorr"] == 1
::weve::we've

#If tools["ContCorr"] == 1
::whatd::what'd

#If tools["ContCorr"] == 1
::whatll::what'll

#If tools["ContCorr"] == 1
::whatre::what're

#If tools["ContCorr"] == 1
::whats::what's

#If tools["ContCorr"] == 1
::whatve::what've

#If tools["ContCorr"] == 1
::whens::when's

#If tools["ContCorr"] == 1
::whered::where'd

#If tools["ContCorr"] == 1
::wherere::where're

#If tools["ContCorr"] == 1
::wheres::where's

#If tools["ContCorr"] == 1
::whereve::where've

#If tools["ContCorr"] == 1
::whichs::which's

#If tools["ContCorr"] == 1
::whod::who'd

#If tools["ContCorr"] == 1
::wholl::who'll

#If tools["ContCorr"] == 1
::whos::who's

#If tools["ContCorr"] == 1
::whyd::why'd

#If tools["ContCorr"] == 1
::whyre::why're

#If tools["ContCorr"] == 1
::whys::why's

#If tools["ContCorr"] == 1
::wont::won't

#If tools["ContCorr"] == 1
::wouldnt::wouldn't

#If tools["ContCorr"] == 1
::wouldve::would've

#If tools["ContCorr"] == 1
::yall::y'all

#If tools["ContCorr"] == 1
::youd::you'd

#If tools["ContCorr"] == 1
::youll::you'll

#If tools["ContCorr"] == 1
::youre::you're

#If tools["ContCorr"] == 1
::youve::you've